# Rendering Engine

Sprint 10 converts `renderer-ready-package.json` into render job/status/artifact packages. External renderers run in dry-run/mock mode by default. If FFmpeg is unavailable, the engine creates structured placeholder artifacts instead of silently failing.
